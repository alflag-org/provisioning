[tools]
actionlint = "1.7.12"
shellcheck = "0.11.0"
python = "3.13.3"

[tasks.setup]
description = "Install validation tools and Ansible collections"
run = """
python -m venv .venv
.venv/bin/python -m pip install --requirement requirements-dev.txt
.venv/bin/ansible-galaxy collection install \
  --requirements-file collections/requirements.yml \
  --collections-path collections
"""

[tasks.lint]
description = "Validate workflows and lint all YAML, playbooks, and roles"
run = """
actionlint
.venv/bin/yamllint .
PATH="$PWD/.venv/bin:$PATH" ansible-lint playbooks roles
"""

[tasks.test]
description = "Run behavior and inventory validation tests"
run = ".venv/bin/python -m unittest discover -s tests -p 'test_*.py'"

[tasks.check]
description = "Run repository validation"
depends = ["lint", "test"]
