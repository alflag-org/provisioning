---
- name: Bootstrap new hosts for configuration management
  hosts: default
  gather_facts: true

  roles:
    - role: foundation/bootstrap  # noqa role-name[path]

- name: Bootstrap Atlas runtime on managed hosts
  hosts: default
  gather_facts: true

  roles:
    - role: atlas_host
