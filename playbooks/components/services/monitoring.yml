---
- name: Converge Zabbix monitoring service
  hosts: svc_monitoring
  gather_facts: true

  roles:
    - role: services/monitoring  # noqa role-name[path]
      when: services_monitoring_enabled | default(false)
